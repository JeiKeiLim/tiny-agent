# Copy to .env and fill in local values. Do not commit .env.
# The Kestrel scripts read process environment variables; export these values
# before running internal LLM data prep, e.g. `set -a; source .env; set +a`.
KESTREL_LLM_API_BASE=
KESTREL_LLM_API_KEY=
KESTREL_LLM_MODEL=
